// Google Analytics 4 (GA4) setup — shared across every page on the site.
// Measurement ID lives here in one place so it only needs updating once.
window.dataLayer = window.dataLayer || [];
function gtag() { dataLayer.push(arguments); }
gtag('js', new Date());
gtag('config', 'G-NV3RTC7XXK');
